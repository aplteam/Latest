 R←Test_120(stopFlag batchFlag);⎕TRAP;data;expected;noOf
⍝ Specify the workspace (root, #)
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')
 R←T._Failed

 noOf←≢data←##.Latest.Run'#' '' 0 ¯999
 →T.PassesIf 7<noOf

 data←##.Latest.Run'#' '' 0 ¯999
 →T.PassesIf noOf=≢data

 R←T._OK
⍝Done
