 R←Test_140(stopFlag batchFlag);⎕TRAP;data;expected;projectPath
⍝ "Stats" for an acre project
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')

 :If 0=≢⎕SE.acre.Projects''
     →0,R←T._NotApplicable
 :EndIf
 ⍝ We assume here that the open project is "Latest"

 R←T._Failed

 data←##.Latest.Run'' 1 1 0 ⍬              ⍝ The default project
 →T.PassesIf 0<≢data

 projectPath←↑(⎕SE.acre.Projects'')[0;1]   ⍝ A specific project
 data←##.Latest.Run projectPath 1 1 0 ⍬
 →T.PassesIf 0<≢data
 →T.PassesIf 0<+/data[;1]

 R←T._OK
⍝Done
