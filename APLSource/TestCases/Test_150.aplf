 R←Test_150(stopFlag batchFlag);⎕TRAP;data;projectPath;expected
⍝ List for an acre project
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')

 :If 0=≢⎕SE.acre.Projects''
 :OrIf 0=##.⎕NC'AcreConfig'
     →0,R←T._NotApplicable
 :EndIf
 ⍝ We assume here that the open project is "Latest"

 R←T._Failed

 data←##.Latest.Run'' 1 0 0 ⍬
 →T.PassesIf∧/'#.Latest.'{⍺∘≡¨(≢⍺)↑¨⍵[;0]}data

 projectPath←##.AcreConfig.ProjectFolder,'APLSource'
 data←##.Latest.Run projectPath 1 0 0 999
 →T.PassesIf 0<≢data
 expected←'Latest\Version.aplf' 'Latest\Run.aplf'
 →T.PassesIf∧/expected∊{⍵/⍨0<≢¨0⊃¨⎕NPARTS¨⍵}data[;0]

 R←T._OK
⍝Done
