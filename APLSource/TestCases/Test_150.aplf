 R←Test_150(stopFlag batchFlag);⎕TRAP;data;expected;projectPath
⍝ List for an acre project
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')

 :If 0=≢⎕SE.acre.Projects''
     →0,R←T._NotApplicable
 :EndIf
 ⍝ We assume here that the open project is "Latest"

 R←T._Failed

 data←##.Latest.Run'' '' 0 1 ⍬
 →T.PassesIf∧/'#.Latest.'{⍺∘≡¨(≢⍺)↑¨⍵[;0]}data

 projectPath←↑(⎕SE.acre.Projects'')[0;1]
 data←##.Latest.Run''projectPath 0 1 ⍬
 →T.PassesIf 0<≢data
 →T.PassesIf∧/'Latest' 'TestCases'∊∪{⍵↑⍨⌊/⍵⍳'/\'}¨data[;0]

 R←T._OK
⍝Done
