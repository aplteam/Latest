 ind←SelectProject projects;flag;answer
 flag←0
 ind←¯1
 :Repeat
     ⎕←'There is more than one project open:'
     ⎕←{⊃(⍕¨1+⍳≢⍵),¨'.',¨↓⍵}projects
     ⎕←'   Which project? (q=quit)'
     answer←⍞
     →((answer~' ')∊'qQ')/0
     ind←↑A.ToNum answer
     :If 1=≢ind
         :If ind∊1+⍳≢projects
             flag←1
             ind-←1
         :EndIf
     :EndIf
 :Until flag
⍝Done
